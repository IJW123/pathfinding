//! Sprite-catalog knobs.

/// Baked manifest path, relative to the working directory (workspace root under `cargo run`).
/// Must match `spritebake`'s output path.
pub const MANIFEST_PATH: &str = "assets/sprite_manifest.ron";
